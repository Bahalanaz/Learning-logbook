The progress for Day 8 is located in a separate repository:

👉 django-rest-student-management-API

This decision was made to separate it as a standalone project since it represents a major milestone in my backend development journey. It marks the transition from learning Django fundamentals to building a structured REST-style backend system with authentication, ORM-based CRUD operations, and JSON API design.

This project serves as a foundational backend system that demonstrates real-world API development practices and scalable backend architecture principles.